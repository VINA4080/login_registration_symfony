<nav class="navbar navbar-expand-lg navbar-expand-md navbar-expand-sm navbar-dark bg-dark">
    <div class="container-fluid">
      
      <a class="navbar-brand text-white" href="#">LOGO</a>
      <li class="nav-link">
        <a class="nav-link active text-white" href="{{ path('app_home_page') }}">Home
          <span class="visually-hidden"></span>
        </a>
      </li>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"><i class="fa fa-skype" aria-hidden="true"></i></span>
      </button>
      <div class="collapse navbar-collapse d-flex justify-content-end" id="navbarColor01">
        <ul class="navbar-nav me-auto">
          {# <li class="nav-item">
            <a class="nav-link active text-white" href="#">Liste
              <span class="visually-hidden"></span>
            </a>
          </li> #}
          {% if app.user %}
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Menu</a>
              <div class="dropdown-menu">
                <a class="dropdown-item" href="#">Accueil</a>
                <a class="dropdown-item" href="#">Qui sommes-nous</a>
                <a class="dropdown-item" href="#">Contact</a>
              <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
                  {# You are logged in as {{ app.user.userIdentifier }} #}
            </li>
              {% else %}
                <li class="nav-link"><a class="text-white text-decoration-none" href="{{ path('app_register') }}">Register</a></li>
                <li class="nav-link"><a class="text-white text-decoration-none" href="{{ path('app_login') }}">Login</a></li>
          {% endif %}  
        </ul>
      </div>
    </div>
    
</nav>